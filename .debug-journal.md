# Inline field color debug journal

- Created for this task; remove before handoff.
- Runtime reused read-only: existing dev server on port 5173 (not started by this task).
- Temporary artifacts planned: `/tmp/furraid-field-colors.png`; remove before handoff.
- Hypotheses: live CSS state/focus tint; browser autofill/default appearance; export-only GIF palette or canvas clone difference.
- Confirmed runtime: empty fields are transparent; filled fields become `rgb(247, 251, 255)` through `.inline-field.is-filled`. Browser appearance stayed `auto`; GIF code is not involved.
- Existing template pixel samples are also `[247,251,255]`, so the visible seam comes from a separate opaque CSS layer over the scaled raster rather than a changed color token.
- Process planned: production preview on `127.0.0.1:4173`; stop before handoff.
- Existing browser QA writes ignored `.omo/evidence/self-card-redesign` captures and `/tmp/furraid-redesign-{valid,small}.png` fixtures.
- Confirmed forced Chrome autofill state: background `rgb(232,240,254)`, text fill `rgb(0,0,0)`. Temporary capture: `/tmp/furraid-field-autofill-before.png`.
- Added scope: GIF-only circular progress indicator driven by encoded frame count for Save and Share.
- User clarified the opaque filled/focus surface is intentional because it covers template placeholder icons. Restored it; only browser autofill colors are overridden.
- Final progress visual observed at 50%: viewport-wide translucent blur, transparent white ring, centered percentage, no opaque circle or label background.
