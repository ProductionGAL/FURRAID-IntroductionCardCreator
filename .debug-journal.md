# Debug Journal — Firefox private save regression
Started: 2026-07-20T21:24:05+09:00
Goal: Restore image saving after the X share/rendering changes without reintroducing white exported photos.

## Environment snapshot (Phase 0)
- Runtime: Bun 1.3.14, Node 24.16.0, Vite 8.1.5, React 19.2.7
- Entry: `bun run dev` / `bun run build`
- Ports: existing user-owned Vite servers on 5173 and 3000; debugger ports 9229/9230 unused
- Git HEAD: ac4d572c5e62611a601ac401536bc91b34957825; pre-existing change in `src/components/InlineCardEditor.tsx`
- References read: Node runtime, Playwright CLI, setup, investigation, TypeScript README

## Hypotheses
1. [INVESTIGATING] The white-photo fix decodes the already-previewed blob a second time while html-to-image is rendering, and Firefox private mode rejects or exhausts memory. Distinguishing evidence: the `b7ec1d4` diff adds `loadImage(photo.url)` plus `decode()`; forcing blob `decode()` rejection should toggle the save failure. If true, fix is: reuse preview.
2. [INVESTIGATING] The X share commit changed successful canvas output into a rejected `canvasToPngBlob` path. Distinguishing evidence: `0e6ce87` changes only callback wrapping and the callback receives null during the repro. If true, fix is: encode fallback.
3. [INVESTIGATING] Immediate Blob URL revocation prevents Firefox private downloads. Distinguishing evidence: rendering and Blob creation succeed, but no download event occurs until revocation is deferred. If true, fix is: defer revoke.
4. [INVESTIGATING] The unchanged html-to-image foreignObject conversion is the actual failure. Distinguishing evidence: editor layer conversion rejects before photo decode/PNG generation. If true, fix is: reduce DOM render.

## Failed hypothesis round counter
- Round 1: pending

## Artifacts to revert
- [ ] `.debug-journal.md` — remove before completion.
- [ ] Browser regression test edits — promote as permanent regression coverage if they correctly distinguish the bug.
- [ ] QA server/session/artifacts — stop and remove before completion.

## Findings
- Git history: `0e6ce87` added X sharing and Promise-based PNG conversion without changing `renderCard`.
- Git history: `b7ec1d4` later removed the photo from the html-to-image clone, then added a concurrent second blob-image load and explicit `decode()` before direct canvas drawing.
- Current preview already owns a loaded `<img class="inline-card__photo ...">` backed by the same object URL.

## Final fix
- Pending.
